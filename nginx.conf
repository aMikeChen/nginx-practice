events {
}


http {
    include mime.types;

    server {
        listen 80;
        server_name localhost;

        root /html/;

        try_files /hi /about;

        location /about {
            return 200 "About me.";
        }
    }
}
